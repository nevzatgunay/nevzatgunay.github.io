---
title: Learn Redux
description: Basic Redux usage
date: '2018-12-24'
image: '/images/learn-redux.png'
---

<Image
  alt={`learn-redux`}
  src={`/images/learn-redux.png`}
  width={1440}
  height={960}
  priority
/>

The repo shows that the usage of Redux and its mechanism in React.

The project has just one `html` file and there is no `package.json`, `CRA`, or anything else. Clone or download the repo, and just double click the `index.html` file.

- [GitHub](https://github.com/nevzatgunay/learn-redux)
